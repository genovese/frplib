# as_float

`as_float` converts a vector tuple with high-precision components to
a vector tuple of standard floats. This is mostly used when frplib
calculations are fed to other functions or libraries.
