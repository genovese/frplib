# Distinct

`Distinct` is a condition that returns true if all of its components are distinct.
It accepts a tuple of any dimension and returns a Boolean tuple (0 or 1).

Examples

+ `Distinct(1, 2, 3)` => <1>
+ `Distinct(1, 2, 1)` => <0>
+ `Distinct(1)` => <1>
+ `Distinct()` => <1>
