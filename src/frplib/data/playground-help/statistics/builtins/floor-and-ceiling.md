# Floor and Ceiling

The `Floor` and `Ceil` statistics round a number to the nearest integer
in different ways:

+ `Floor(x)` returns the largest integer `<= x`
+ `Ceil(x)` returns the smallest integer `>= x`

Both accept and return a scalar tuple. The given an error
if given a non-scalar tuple.
