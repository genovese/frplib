# Descending

The `Descending` statistic accepts a tuple of any dimension and returns a
tuple of the same dimension with the components sorted in descending order.

Examples:

+ `Descending(11, -2, 33, 4)` => <33, 11, 4, -2>
+ `Descending(1, -1, 0)` => <1, 0, -1>
+ `Descending()` => <>
