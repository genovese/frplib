# Ascending

The `Ascending` statistic accepts a tuple of any dimension and returns a
tuple of the same dimension with the components sorted in ascending order.

Examples:

+ `Ascending(11, -2, 33, 4)` => <-2, 4, 11, 33>
+ `Ascending(1, -1, 0)` => <-1, 0, 1>
+ `Ascending()` => <>
