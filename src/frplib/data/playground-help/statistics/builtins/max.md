# Max

The `Max` statistic accepts a tuple of any dimension and returns the
maximum of all components. If the tuple is empty, `Max` returns -infinity.

Examples:

+ `Max(1, 2, 3, 4)` => <4>
+ `Max(1, -1, 0)` => <1>
+ `Max()` => <-Infinity>
