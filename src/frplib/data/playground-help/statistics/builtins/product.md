# Product

The `Product` statistic accepts a tuple of any dimension and returns the
product of all components. If the tuple is empty, `Product` returns 1.

Examples:

+ `Product(1, 2, 3, 4)` => <24>
+ `Product(1)` => <1>
+ `Product()` => <1>
