# Min

The `Min` statistic accepts a tuple of any dimension and returns the
minimum of all components. If the tuple is empty, `Min` returns infinity.

Examples:

+ `Min(1, 2, 3, 4)` => <1>
+ `Min(1, -1, 0)` => <-1>
+ `Min()` => <Infinity>

