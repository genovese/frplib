# Sum

The `Sum` statistic accepts a tuple of any dimension and returns the
sum of all components. If the tuple is empty, `Sum` returns 0.

Examples:

+ `Sum(1, 2, 3, 4)` => <10>
+ `Sum(1)` => <1>
+ `Sum()` => <0>

