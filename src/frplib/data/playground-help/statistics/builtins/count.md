# Count and Dim

`Count` and `Dim` are two names for the same statistic. It accepts a tuple
of any dimension and returns the number of components in its input tuple.

+ `Count(1, 2, 3, 4)` => `<4>`
+ `Dim(1)` => `<1>`
+ `Dim()` => `<0>`
+ `Count(1, 2, 3, 4, 5)` => `<5>`
