# scalar_fn

`scalar_fn(s)` for a scalar statistic `s` returns a regular scalar function,
i.e., one returning a number not a tuple.

Example: `scalar_fn(Sum)` is analogous to the Python builtin `sum`.
