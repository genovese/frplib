# independent_join

The function `independent_join` accepts a list of Kinds or FRPs and forms their 
independent join.  Specifically, the single argument should be an iterable
object (like a list) containing either all Kinds or all FRPs. It returns
the independent join of the objects in the list.
