# Decorators

Decorators provide a way to attach special capabilities
or make modifications to functions in way that is readable
and modular.

ATTN

Example is the decorator `flexible_inputs` that is defined
by default in the playground.

ATTN
